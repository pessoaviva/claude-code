{
  "name": "fintrack-pro",
  "version": "1.0.0",
  "private": true,
  "description": "FinTrack Pro — sistema financeiro completo (ganhos, gastos, patrimônio, ações, vendas, metas, relatórios)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "build:all": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server"
  },
  "license": "MIT"
}
